// https://pro-rat-74.clerk.accounts.dev

const authConfig = {
  providers: [
    {
      domain: "https://pro-rat-74.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};

export default authConfig;
